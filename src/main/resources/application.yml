server:
  port: 8080

spring:
  application:
    name: smart-campus-navigation
  datasource:
    url: jdbc:mysql://localhost:3306/smart_campus_navigation?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
    username: root
    password: 2004123po
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 5000
      validation-timeout: 3000
      initialization-fail-timeout: 5000
  thymeleaf:
    cache: false

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

amap:
  js-key: 8d384515cb0bcac22a06862e06e600dc
  security-js-code: c56cdb1e8c7ff71775749e8e0e40859f
  web-service-key: ebef09fb48f3dc8140a8ed544f0bcc96

qweather:
  host: nc2pg7kpbt.re.qweatherapi.com
  auth-type: api-key
  api-key: 68404ed41b1e4ba492943aa90d680885
  location: 118.7169,32.2072
  lang: en
  unit: m

# DeepSeek is called only by the backend proxy.
# API key priority: environment variable -> deepseek_api.txt.
deepseek:
  enabled: ${DEEPSEEK_ENABLED:true}
  base-url: ${DEEPSEEK_BASE_URL:https://api.deepseek.com}
  api-key: ${DEEPSEEK_API_KEY:}
  api-key-file: ${DEEPSEEK_API_KEY_FILE:deepseek_api.txt}
  model: ${DEEPSEEK_MODEL:deepseek-chat}
  connect-timeout-ms: 3000
  read-timeout-ms: 8000
  temperature: 0.2
